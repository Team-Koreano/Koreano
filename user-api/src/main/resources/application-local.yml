spring:
  application:
    name: "user-api"
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: sa
    password:
    hikari:
      maximum-pool-size: 100 #최대 pool 크기
      minimum-idle: 10 #최소 pool 크기
      idle-timeout: 600000 #연결위한 최대 유후 시간
      max-lifetime: 1800000 #반납된 커넥션의 최대 수명
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
    show-sql: true
  sql:
    init:
      mode: always
  data:
    redis:
      host: 127.0.0.1
      port: 6379
  config:
    activate:
      on-profile: local
jwt:
  valid:
    access: ENC(rKslQE0s5b/jAfdLX3voxA==)
    refresh: ENC(00xp0hXb+x78a5//Ht+1Gw==)
  secret: ${SECRET_KEY_SOURCE}

payment-service:
  url: http://localhost:8085/api/internal/beanpay/v1